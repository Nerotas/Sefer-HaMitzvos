# AWS Lambda Dependencies for Mitzvah Bot

# For Twilio WhatsApp API
twilio>=9.8.0

# HTTP requests (included with twilio but explicit)
requests>=2.32.0

# Note: python-dotenv, schedule not needed in Lambda
# Environment variables managed by AWS Lambda
# Scheduling handled by CloudWatch Events